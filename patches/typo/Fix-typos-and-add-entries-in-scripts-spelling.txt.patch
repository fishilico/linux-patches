From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Nicolas Iooss <nicolas.iooss_linux@m4x.org>
Date: Fri, 22 May 2015 10:29:47 +0800
Subject: [PATCH] {TYPO} Fix typos and add entries in scripts/spelling.txt

---
 include/scsi/scsi_cmnd.h    | 2 +-
 scripts/spelling.txt        | 2 ++
 sound/pci/hda/hda_generic.c | 2 +-
 3 files changed, 4 insertions(+), 2 deletions(-)

diff --git a/include/scsi/scsi_cmnd.h b/include/scsi/scsi_cmnd.h
index b379f93a2c48..75f5276df6a2 100644
--- a/include/scsi/scsi_cmnd.h
+++ b/include/scsi/scsi_cmnd.h
@@ -63,7 +63,7 @@ struct scsi_cmnd {
 	struct list_head list;  /* scsi_cmnd participates in queue lists */
 	struct list_head eh_entry; /* entry for the host eh_cmd_q */
 	struct delayed_work abort_work;
-	int eh_eflags;		/* Used by error handlr */
+	int eh_eflags;		/* Used by error handler */
 
 	/*
 	 * A SCSI Command is assigned a nonzero serial_number before passed
diff --git a/scripts/spelling.txt b/scripts/spelling.txt
index eb38f49d4b75..f7d032b2652f 100644
--- a/scripts/spelling.txt
+++ b/scripts/spelling.txt
@@ -520,6 +520,8 @@ guarentee||guarantee
 halfs||halves
 hander||handler
 handfull||handful
+handl||handle
+handlr||handler
 hanled||handled
 happend||happened
 harware||hardware
diff --git a/sound/pci/hda/hda_generic.c b/sound/pci/hda/hda_generic.c
index 2842c82363c0..44716a29d5ae 100644
--- a/sound/pci/hda/hda_generic.c
+++ b/sound/pci/hda/hda_generic.c
@@ -847,7 +847,7 @@ static hda_nid_t path_power_update(struct hda_codec *codec,
 			snd_hda_codec_write(codec, nid, 0,
 					    AC_VERB_SET_POWER_STATE, state);
 			changed = nid;
-			/* all known codecs seem to be capable to handl
+			/* all known codecs seem to be capable to handle
 			 * widgets state even in D3, so far.
 			 * if any new codecs need to restore the widget
 			 * states after D0 transition, call the function
-- 
