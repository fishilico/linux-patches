From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Nicolas Iooss <nicolas.iooss_linux@m4x.org>
Date: Sat, 18 Nov 2017 13:24:40 +0100
Subject: [PATCH] {PRINTF} gpu/drm/amd/display: add __printf attributes to
 logging functions

---
 drivers/gpu/drm/amd/display/include/logger_interface.h | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/gpu/drm/amd/display/include/logger_interface.h b/drivers/gpu/drm/amd/display/include/logger_interface.h
index 28dee960d509..c0b0f6311090 100644
--- a/drivers/gpu/drm/amd/display/include/logger_interface.h
+++ b/drivers/gpu/drm/amd/display/include/logger_interface.h
@@ -50,17 +50,17 @@ void dm_logger_write(
 		struct dal_logger *logger,
 		enum dc_log_type log_type,
 		const char *msg,
-		...);
+		...) __printf(3, 4);
 
 void dm_logger_append(
 		struct log_entry *entry,
 		const char *msg,
-		...);
+		...) __printf(2, 3);
 
 void dm_logger_append_va(
 		struct log_entry *entry,
 		const char *msg,
-		va_list args);
+		va_list args) __printf(2, 0);
 
 void dm_logger_open(
 		struct dal_logger *logger,
@@ -75,7 +75,7 @@ void dc_conn_log(struct dc_context *ctx,
 		int hex_data_count,
 		enum dc_log_type event,
 		const char *msg,
-		...);
+		...) __printf(6, 7);
 
 void logger_write(struct dal_logger *logger,
 		enum dc_log_type log_type,
-- 
